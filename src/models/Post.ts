export interface Post {
    answer: string,
    requiredPoints: number,
    description: string,
    title:string,
    deadline: string,
    courseId: number,
    parentId: number,
    points: number,
}
