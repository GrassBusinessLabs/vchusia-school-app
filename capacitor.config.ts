import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vchusia-school-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
